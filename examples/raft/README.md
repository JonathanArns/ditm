# raft example
This example's code is heavily based upon [this](https://github.com/eileen-code4fun/SimpleRaft) toy raft implementation. The implementation has been modified to communicate via HTTP, instead of Go channels, enabling the system to run in a distributed environment.

